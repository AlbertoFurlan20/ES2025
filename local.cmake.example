# Copy this file to local.cmake (same directory), then set your machine's path.

# local.cmake is gitignored, so each developer keeps their own without conflicts.
set(RTEMS_LOCAL_PATH /Users/albertofurlan/Developer/PoliMi)
